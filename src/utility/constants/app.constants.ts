// Application-wide constants. Branding/org values are configured via environment
// variables (PRODUCT_NAME, CHURCH_NAME, CHURCH_ADDRESS, DEFAULT_VENUE_NAME) and
// validated by Joi at startup — read them through ConfigService, not process.env.
